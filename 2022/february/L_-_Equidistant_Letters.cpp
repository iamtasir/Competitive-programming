#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    // O_O ;
    ll t;
    cin >> t;
    while (t--)
    {

        string s;
        cin >> s;
        sort(s.begin(), s.end());
        cout << s << "\n";
    }

    return 0;
}
